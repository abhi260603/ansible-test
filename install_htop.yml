---

- hosts: all
  become: true
  gather_facts: yes
  tasks:

  - name: update repository index
    dnf:
      update_cache: yes
    when: ansible_facts["distribution"] == "Fedora"

  - name: install htop package
    dnf:
      name: htop
    when: ansible_facts["distribution"] == "Fedora"
